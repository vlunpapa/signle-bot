# Dev计划容量分析：每分钟1个CA场景

## 场景描述
- **频率**：每分钟都有新的CA需要分析
- **每个CA的处理流程**：
  1. 获取最近10分钟交易历史
  2. 构造10根1分钟K线
  3. 执行策略分析（外源性二段告警等）
  4. 发出告警信号

## Dev计划限制

### 速率限制
- **Enhanced/DAS API**: 10 req/s
- **RPC API**: 50 req/s
- **Webhooks**: 50个
- **WebSocket连接**: 150个并发

### Credits限制
- **月度Credits**: 10M credits/月
- **估算消耗**（基于Helius文档）：
  - Enhanced Transactions API: 约100-500 credits/请求（取决于返回数据量）
  - RPC getTransaction: 约10-50 credits/请求
  - RPC getSignaturesForAddress: 约5-20 credits/请求

## 容量计算

### 速率角度
- **每分钟1个CA** = 每小时60个CA = 每天1,440个CA
- **每个CA需要**：
  - 理想情况：1次Enhanced Transactions API调用（拉10分钟历史）
  - 最坏情况：1次getSignaturesForAddress + 20次getTransaction（RPC fallback）

**速率评估**：
- Enhanced API路径：每分钟1次 = 0.017 req/s << 10 req/s ✅ **完全够用**
- RPC fallback路径：每分钟21次 = 0.35 req/s << 50 req/s ✅ **完全够用**

### Credits消耗角度
假设每个CA分析：
- **Enhanced路径**：1次调用 × 300 credits（平均值） = 300 credits/CA
- **RPC fallback路径**：1次getSignatures + 20次getTransaction = 5 + 20×30 = 605 credits/CA

**月度消耗估算**：
- Enhanced路径：1,440 CA/天 × 30天 × 300 credits = **12.96M credits/月** ❌ **超出10M限制**
- RPC fallback路径：1,440 CA/天 × 30天 × 605 credits = **26.14M credits/月** ❌ **严重超出**

**结论**：如果完全依赖"每次CA都拉10分钟历史"，Dev计划的10M credits**不够用**。

## 关键问题

### 1. Credits消耗是主要瓶颈
- 每分钟1个CA = 每天1,440个CA
- 即使每次只用Enhanced API（300 credits），月度消耗约13M，**超出10M限制**
- 如果走RPC fallback，消耗会翻倍甚至更多

### 2. 突发流量风险
- 如果某段时间内多个CA同时到达（比如市场波动时）
- 虽然速率限制（10 req/s Enhanced）理论上可以处理，但：
  - 多个CA并发分析会导致响应时间增加
  - 可能触发rate limit，需要等待
  - Credits消耗会加速

### 3. 时间延迟累积
- 如果每个CA分析需要2秒，每分钟1个CA理论上可以处理
- 但如果出现突发（比如1秒内来3个CA），会导致：
  - 队列堆积
  - 延迟增加
  - 可能无法满足"2秒内告警"的SLA

## 解决方案建议

### 方案A：缓存机制（推荐）
**核心思路**：不要每次都拉10分钟历史，而是维护滑动窗口缓存

**实现**：
1. 对每个被监控的CA，启动持续监控任务
2. 每分钟拉取1根最新的1分钟K线
3. 维护一个长度为10的K线环形缓冲区
4. 当CA触发时，直接从缓存读取10根K线，无需拉历史

**优势**：
- Credits消耗大幅降低：每分钟1次调用（拉最新1根K线）vs 每次CA都拉10分钟历史
- 延迟更低：缓存读取是毫秒级，无需等待API响应
- 更稳定：不依赖历史API的可用性和延迟

**Credits消耗**：
- 持续监控：1,440 CA × 30天 × 100 credits（单次K线） = **4.32M credits/月** ✅ **在10M限制内**

### 方案B：批量处理 + 限流
**核心思路**：对CA进行批量处理，避免突发流量

**实现**：
1. 使用队列缓冲CA请求
2. 按批次处理（比如每5秒处理一批）
3. 对每个批次内的CA，并行拉取历史数据
4. 使用限流器确保不超过API速率限制

**优势**：
- 可以平滑处理突发流量
- 更好地利用API速率限制

**劣势**：
- Credits消耗仍然较高（如果每次都拉历史）
- 延迟可能增加（需要等待批次）

### 方案C：升级计划
如果必须每次都拉10分钟历史，且CA频率很高：
- **Business计划**：$99/月，50M credits/月，Enhanced API 50 req/s
- **Professional计划**：$499/月，250M credits/月，Enhanced API 100 req/s

## 推荐方案

**强烈推荐方案A（缓存机制）**：

1. **Credits消耗可控**：4.32M/月 vs 12.96M/月（降低67%）
2. **延迟更低**：缓存读取 < 10ms vs API拉取 500ms-2s
3. **更稳定**：不依赖历史API的可用性
4. **符合你的需求**："最快速度计算出量价关系触发策略并告警"

**实现优先级**：
1. ✅ 立即实现：缓存机制 + 持续监控
2. ⚠️ 中期优化：Enhanced Transactions API的正确使用（避免RPC fallback）
3. 📊 长期监控：Credits消耗统计，确保不超限

## 总结

**Dev计划在"每分钟1个CA"场景下的可行性**：

| 指标 | Enhanced路径 | RPC Fallback路径 | 缓存机制 |
|------|-------------|------------------|----------|
| 速率限制 | ✅ 够用 | ✅ 够用 | ✅ 够用 |
| Credits消耗 | ❌ 超出（13M/月） | ❌ 严重超出（26M/月） | ✅ 够用（4.3M/月） |
| 延迟 | ⚠️ 500ms-2s | ❌ 2s-20s | ✅ < 10ms |
| 稳定性 | ⚠️ 依赖API | ❌ 不稳定 | ✅ 高稳定 |

**最终建议**：**必须使用缓存机制**，否则Dev计划的10M credits无法支撑每分钟1个CA的长期运行。
